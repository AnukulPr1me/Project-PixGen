import { StyleSheet, Text, View } from 'react-native'
const Onboarding_UI = (navigation) => {
  return (
    <View>
      <Text>Onboarding_UI</Text>
    </View>
  )
}
export default Onboarding_UI
const styles = StyleSheet.create({})